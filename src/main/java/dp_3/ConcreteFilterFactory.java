/*
package dp_3;



public class ConcreteFilterFactory extends FilterFactory {

    @Override
    public FilterStrategy createFilter(FilterType type, boolean hasInverseBoundaries) {
        if (hasInverseBoundaries) {
            return new InverseFilter(type);
        } else{
            return new Filter(type);
        }
    }
}
*/
